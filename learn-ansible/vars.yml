- name: Print sample text
  hosts: localhost
  vars: 
  	URL_PLAY: play.example.com       
  tasks: 
   	- name: Prints a statement
      ansible.builtin.debug:
        msg: URL_PLAY  - {{ URL_PLAY  }}        

    - name: Prints a statement
      ansible.builtin.debug:
        msg: URL_PLAY  - {{ URL_PLAY }}

	  - name: Task variable		
      ansible.builtin.debug:
        msg: URL_TASK  - {{ URL_TASK }} 
      vars:
		    URL_TASK : mytask        

    - name: Prints a statement
      ansible.builtin.debug:
        msg: URL_PLAY  - {{ URL_TASK }}   

